# Versions

This table is the source of truth for the Release workflow's
branch ↔ minor validation. The reconciler also reads it from the
default branch when computing bump targets.

| Branch | Minor |
|--------|-------|
| main   | v0.5  |
